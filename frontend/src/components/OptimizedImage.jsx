import React, { lazy, Suspense } from "react"; // Importer le composant LazyImage de manière différée const LazyImage = lazy(() => import("./common/LazyImage")); // Composant pour gérer le chargement différé des images const OptimizedImage = (props) => { return ( <Suspense fallback={<div className={`bg-gray-200 ${props.className || ""}`} style={{aspectRatio: "16/9"}} />}> <LazyImage {...props} /> </Suspense> ); }; export default OptimizedImage;
